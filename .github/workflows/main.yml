name: SYNC_REPO

on:
  schedule:
    - cron:  '0 1 * * *'
  push:
    branches:
      - main

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
